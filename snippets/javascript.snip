snippet describe
    describe('${1:what}', () => {
        ${0:TARGET}
    })

snippet it
    it('${1:does what and when}', () => {
        assertThat(${0:TARGET})
    })

snippet beforeEach
    beforeEach(() => {
        ${0:TARGET}
    })

snippet anonymous_function
options word
alias f
    (${1}) => {
        ${0:TARGET}
    }


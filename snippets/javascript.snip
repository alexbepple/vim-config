snippet describe
    describe('${1:what}', () => {
        ${0:TARGET}
    })

snippet it
    it('${1:does what and when}', () => {
        assertThat(${0:TARGET})
    })

snippet beforeEach
    beforeEach(() => {
        ${0:TARGET}
    })

snippet anonymous_function
options word
alias _f
    function (${1:#:argument}) {
        ${0:TARGET}
    }

snippet anonymous_function_without_parameters
options word
alias __f
    () => {
        ${0:TARGET}
    }

